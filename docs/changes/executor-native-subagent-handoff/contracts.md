# Contracts

## Contract artifacts

- Artifact: executor native subagent handoff contract
- Path: `docs/contracts/executor-native-subagent-handoff.md`
- Type: documentation invariant plus executable CLI/gate tests

Primary contract: [executor-native-subagent-handoff](../../contracts/executor-native-subagent-handoff.md)

The reviewer must verify that governed runner dispatch no longer uses platform/process executors and that native handoff lifecycle records are prepareable, spawn-recordable, parseable, and gateable.
