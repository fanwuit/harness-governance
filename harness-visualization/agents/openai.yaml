interface:
  display_name: "Harness Visualization"
  short_description: "Show harness layer, ready queue, runner, and verification status"
  default_prompt: "Use $harness-visualization to generate a read-only status dashboard from queue, checkpoint, task packet, and runner records."
